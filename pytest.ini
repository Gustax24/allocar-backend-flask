[pytest]
addopts = -q
testpaths = tests
markers =
    unit: tests unitaires (services, repository, fonctions pures)
    integration: tests d’intégration API (client Flask)
filterwarnings =
    ignore:.*broker_connection_retry.*:UserWarning
    ignore:.*CPendingDeprecationWarning.*:Warning
